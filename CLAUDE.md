# 社内UI開発 ワークスペース

## このワークスペースについて
`C:\UI` は社内UIツール開発の共通ワークスペースです。
複数のUIプロジェクトが `official/` または `mockup/` 配下に並列で存在します。

## ディレクトリ構成
```
C:\UI/
├── .claude/          ← ハーネス設定（全プロジェクト共通）
├── official/         ← 本番稼働するUI群（ソース管理・レビュー必須）
│   ├── starter-kit/  ← 新規プロジェクトのテンプレート
│   └── project-xxx/  ← 各UIプロジェクトのルート（Next.jsアプリ）
└── mockup/           ← 検証・探索用のモックアップUI
```

## 起動に関する重要ルール
**必ず `C:\UI`（このディレクトリ）から Claude Code を起動すること。**
各UIのプロジェクトフォルダから起動すると `.claude/` のハーネスフックが無効になります。

```bash
# 正しい起動方法
cd C:\UI
claude --project official/project-xxx
```

## ハーネスの動作概要
| タイミング | 動作 |
|---|---|
| ファイル書き込み直前 | ガバナンスファイルへの書き込みをブロック |
| ファイル書き込み直後 | .clauderules / CLAUDE.md 準拠チェック（即時フィードバック） |
| タスク完了時 | 全チェック（tsc + ESLint + 準拠チェック）→ 通過で feature/ へ自動コミット |

## 変更禁止ファイル（ガバナンスファイル）
以下は自動ブロックされます。変更が必要な場合は feature/ ブランチでPRを提出してください。
- `CLAUDE.md`（このファイル含むすべての CLAUDE.md）
- `.clauderules`
- `tailwind.config.ts`（デザイントークン）
- `components.json`（shadcn/ui 設定）
- `tsconfig.json`

## 2トラック体制
| | mockup | official |
|---|---|---|
| 目的 | 探索・検証 | 本番稼働 |
| 環境 | localhost のみ | サーバー・クラウド |
| ルール適用 | なし | CLAUDE.md 必須 |
| git 管理 | なし | 必須 |
| レビュー | なし | PR必須 |

## mockup から official への昇格プロセス (Promotion Process)

探索・検証段階のモックアップ（`mockup/`）から、本番稼働用（`official/`）へとコードやコンポーネントを移行（昇格）する際は、以下のプロセスに従ってください。

### 1. 昇格の判断基準 (When)
- ビジネス部門やエンドユーザーによるモックアップの検証・要件定義が完了し、本番実装が承認されたとき。
- 複数のダッシュボード（またはプロジェクト）で、同様のUIパターンやロジックを再利用する必要があるとき。

### 2. 役割と責任 (Who)
- **開発者 (Engineer / BP)**: モックアップのHTML/JSコードをNext.js/TypeScriptの厳格な型定義とshadcn/uiに基づいたコンポーネントへと変換し、実装します。
- **コンポーネントレビュアー**: 昇格したコードが `official` のコーディングルール（`.clauderules`）を満たしていること、汎用的な `Props` 設計になっていることを確認し、PRをレビュー・承認します。

### 3. 具体的な昇格手順 (How)
1. **昇格コマンドの実行**:
   ワークスペースルートから `claude` を起動し、カスタムコマンド `/promote-component` を用いて、モックアップコードから `official` 用のコンポーネントを自動生成します。
   ```bash
   # 例
   /promote-component mockup/20260601_sales_dashboard/public/index.html の売上グラフ部分
   ```
2. **品質基準の適合**:
   生成されたコンポーネント（例: `components/patterns/sales-chart.tsx`）が、以下の条件を満たしているか手動確認・調整します。
   - インラインスタイル（`style={{}}`）が使用されていないこと。
   - すべての Props と引数に厳格な型定義が付いていること（`any` の禁止）。
   - 変更されたコードが `npm run type-check` および `npm run lint` をパスすること。
3. **プルリクエストの作成**:
   全チェック通過後、自動的に作成される `feature/{project}-{date}` ブランチを Git にプッシュし、コンポーネントレビュアーに対してPRを作成します。レビュー・承認を経て `master` へマージします。

## official プロジェクトのコーディングルール
各プロジェクトフォルダ直下の `CLAUDE.md` に記載されています。
`official` 配下で作業する際は、必ず各プロジェクトのルールを参照して開発を行ってください。
